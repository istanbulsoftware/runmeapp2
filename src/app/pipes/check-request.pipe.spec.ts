import { CheckRequestPipe } from './check-request.pipe';

describe('CheckRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
